
  interface Metadata {
    version: string
    description: string
    name: string
  }
  const metadata: Metadata = {
    version: "0.0.1",
    description: "Bsale cli for fast query data through terminal",
    name: "bsale-cli",
  }

  export default metadata
  